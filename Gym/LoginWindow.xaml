<Window x:Class="Gym.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gym"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#C1E3E3" Offset="0.0"/>
                <GradientStop Color="DarkSlateBlue"  Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="280" Padding="20" CornerRadius="10" Background="Transparent">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >

                <TextBlock Text="👤" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Gym Admin Login" FontSize="22" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <StackPanel Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="👤" Margin="0,0,5,0"/>
                        <TextBlock Text="User Name" Foreground="White"/>
                    </StackPanel>
                    <TextBox x:Name="txtUserName" Height="30" Background="Transparent" BorderBrush="White" Foreground="White" BorderThickness="0,0,0,1" />
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🔒" Margin="0,0,5,0"/>
                        <TextBlock Text="Password" Foreground="White"/>
                    </StackPanel>
                    <PasswordBox x:Name="txtPassword" Height="30" Background="Transparent" BorderBrush="White" Foreground="White" BorderThickness="0,0,0,1" />
                </StackPanel>

                <Button Content="LOGIN" Height="40" Background="#0078D7" Foreground="White"
                        FontWeight="Bold" BorderThickness="0" Click="Login_Click" Cursor="Hand" />

                <TextBlock x:Name="txtError" Text="" Foreground="Red" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
